io:
  # all files are relative path to the root of the project, not the config_generator folder
  input-template: "litellm-template.yaml"
  output-file:
  output-model-list-file:
  output-model-list-json:
  # input-template: "litellm-template.yaml"
  # output-file: "litellm.yaml"
  # output-model-list-file: "model-list.txt"
  # output-model-list-json: "model-list.json"

config:
  # llm provider specific settings
  llm:
    openai:
      api-key:
      exclude-models: # ['gpt-4', 'gpt-4-32k', 'gpt-4-0613'] # exclude this expensive model from being abuse
    google:
      api-key:
    mistral:
      api-key:
    cohere:
      api-key:
    copilot:
      api-base: # http://copilot-gpt4-service:4000/v1
    openrouter:
      exclude-models: ['openai/gpt-4-32k'] # exclude this expensive model from being abuse
    togetherai:
      exclude-models: 